# Simurg startup

Run the bot with:

```powershell
start_simurg.bat
```

It will:
- create the virtual environment if needed
- install the package
- start the bot automatically

You may still need to complete Telegram login the first time if no valid session is present.
